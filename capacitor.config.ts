import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cinema.application',
  appName: 'Cinema',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
