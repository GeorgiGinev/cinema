import { ScrollHorizontalCenterDirective } from './scroll-horizontal-center.directive';

describe('ScrollHorizontalCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHorizontalCenterDirective();
    expect(directive).toBeTruthy();
  });
});
